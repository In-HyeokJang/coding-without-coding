# 코딩 없이 코딩 · 문서 사이트

## 이 프로젝트가 뭔가

광명시 청년 대상 바이브코딩 커뮤니티 **"코딩 없이 코딩"** 의 참가자용 가이드 문서 사이트.
Astro Starlight로 만들고 GitHub Pages에 배포한다.

**읽는 사람:** 코딩을 모르는 30대 초반 직장인. 폰으로 읽는 경우가 많다.

## 스택

- Astro + Starlight
- GitHub Pages 배포 (GitHub Actions)
- 패키지 매니저: npm

## 명령어

- 개발 서버: `npm run dev`
- 빌드: `npm run build`
- 빌드 미리보기: `npm run preview`

## 폴더 구조

```
src/content/docs/     문서 본문 (md/mdx)
  index.mdx           홈
  core/               도구, 프롬프트, 토큰 설정
  sessions/           1~6회차
astro.config.mjs      Starlight 설정 (사이드바 여기)
.github/workflows/    Pages 배포
```

## 작업 규칙

- 한국어로 답한다.
- 설명은 짧게. 세 줄을 넘기지 않는다.
- 파일을 고치기 전에 무엇을 왜 바꾸는지 한 줄로 먼저 말한다.
- 한 번에 파일 하나씩만 고친다.
- 새 패키지를 설치하기 전에 반드시 물어본다.
- 요청하지 않은 파일은 건드리지 않는다.

## 문서를 쓸 때 지킬 것

- **원본 md의 문장을 함부로 바꾸지 않는다.** 구조와 형식만 Starlight에 맞게 옮긴다.
- 참가자는 코딩을 모른다. 전문 용어를 쓰면 괄호 안에 쉬운 말로 풀어쓴다.
- 회차 문서는 "오늘의 목표 → 오기 전에 → 오늘 하는 것 → 손에 남는 것 → 다음 회차" 순서를 지킨다.
- 프롬프트 예시는 반드시 코드블록에 넣는다. 참가자가 복사해서 쓴다.

### 말투

- **상냥하고 부드럽게 설명하는 말투로 쓴다.** 참가자에게 옆에서 알려주듯이.
- 단정하고 딱딱한 말투를 쓰지 않는다. "~합니다" 로 끊기보다 "~하시면 돼요", "~거든요" 처럼 풀어준다.
- 재촉하거나 겁주지 않는다. 안 된 사람도 괜찮다는 걸 함께 적는다.
- **예외:** 안전 경고(API 키, `.env`, 되돌릴 수 없는 작업)는 단호하게 쓴다.

### 중복

- **글을 고칠 때마다 같은 말이 반복되는지 확인하고 줄인다.** 문서 안에서도, 문서끼리도.
- 같은 안내가 여러 곳에 있으면 **실제로 필요한 자리 한 곳**에만 남기고 나머지는 링크로 넘긴다.
- 회차 문서를 고치면 다른 문서가 그 회차를 잘못 가리키고 있지 않은지 같이 확인한다.

### 강조

중요한 것은 색으로 눈에 띄게 만든다. 굵게만으로는 본문에 묻힌다.

- **다른 문서로 안내할 때는 `>` 인용구를 쓰지 않는다.** 아래 박스를 쓴다. `.md` 에서도 그대로 동작한다.

```html
<div class="doc-link">
	<p class="doc-link-label">함께 보면 좋아요</p>
	<p class="doc-link-to">준비 → AI 팀으로 만들기</p>
	<p class="doc-link-desc">6명짜리 큰 팀, 역할을 다시 짜는 법</p>
</div>
```

- **문장 안 핵심 구절은 `<mark>` 로 감싼다.** 형광펜처럼 칠해진다.
  단, **한 화면에 하나 정도만.** 여러 개 쓰면 강조가 죽는다.
- 스타일은 `src/styles/custom.css` 에 있다. 새 강조를 만들기 전에 이미 있는 걸 먼저 쓴다.

### 가독성

- **폰으로 읽는다는 걸 기준으로 삼는다.**
- 한 문단은 한두 문장까지. 길어지면 줄을 나누고 빈 줄로 띄운다.
- ASCII 그림을 쓰지 않는다. 한글은 글자 폭이 달라 폰에서 어긋난다. 목록이나 표로 바꾼다.
- 코드블록은 줄바꿈이 안 된다. 긴 문장을 코드블록에 넣지 않는다.
- 표는 2열을 기본으로 한다. 칸 안 문장이 길면 표 대신 목록으로 쓴다.
- aside 제목은 짧게. 폰에서 두 줄을 넘기지 않는다.

## 시각화 원칙

- **다이어그램은 4개까지만.** 도구 흐름도, 6회차 진행 곡선, 4회차 도달 3갈래, 결과물 타임라인.
- 그 외에는 표나 글로 충분하다. 다이어그램을 늘리자고 제안하지 않는다.
- Claude Code / Codex 처럼 두 갈래로 갈리는 안내는 탭으로 만든다.
- 경고나 주의는 Starlight의 aside 문법을 쓴다.

## 하지 말 것

- 원본 md에 없는 내용을 지어내지 않는다. 필요하면 물어본다.
- 설치 명령어나 버전 번호를 기억에 의존해 쓰지 않는다. 확실하지 않으면 "확인 필요" 라고 표시한다.
- 트렌디하고 과감한 디자인은 허용한다. 단, 참가자(코딩 모르는 30대 직장인, 폰으로 읽음)가 읽기 어려워지면 안 된다. 화려함보다 가독성이 깨지는 게 진짜 문제다.
